# Config file for Webview Switcher
# If ivvalid values are found, or this file is missing, then the module falls back to the default in parenthesis

# [True/False] Don't do any internet checks. Compatibility is not certain. (default: false)
# NOTE: For offline installs, save browser and webview APKs in /sdcard/WebviewSwitcher/, with name being [webview or browser]_[choice].apk
# For instance, for bromite browser it'd be browser_Bromite.apk
OFFLINE=false
# [True/False] Install the appropriate browser for the webview chosen below (default: true)
BROWSER=true
# [True/False] Install the appropriate webview chosen below (default: true)
WEBVIEW=true
# Choice of browser and webview (default: 0)
# 0 = Bromite
# 1 = Chromium
# 2 = Ungoogled-Chromium
# 3 = Ungoogled-Chromium with extensions support
WHICH=0


